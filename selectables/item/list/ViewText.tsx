import ListItemText, { ListItemTextProps } from '@mui/material/ListItemText';

const ListViewItemText = (props: ListItemTextProps) => (
  <ListItemText {...props} />
);

export default ListViewItemText;
