import ListItemIcon from '@mui/material/ListItemIcon';
import { styled } from '@mui/material/styles';

const ListViewItemIcon = styled(ListItemIcon)(() => ({
  color: 'inherit'
}));

export default ListViewItemIcon;
